// States dictating how civilians will act
public enum CivilianState
{
    // Normal behavior
    NORMAL,
    // Drives around fast to avoid further collision
    FRENZIED,
    // Stays in place
    HALTED
}
